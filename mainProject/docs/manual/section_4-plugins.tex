% Plugins
Sudoken features a pluign-based architecture, allowing you to download and add new puzzle types to the solver. 

Plugins are automatically detected by the system when it's running. Simply copy the \codeCommand{<new\_puzzle\_plugin>.jar} to the \codeCommand{plugins} folder beside \codeCommand{sudoken.jar} to install the new plugin.